# FaceFoundry control panel — runs on YOUR PC (orchestrator + web UI).
# The heavy ML libraries (torch, diffusers, insightface) run on Kaggle, not here.
kaggle==2.2.4                # pin to the version proven to work with KGAT tokens

# Phase 3 web UI
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9   # required by FastAPI for file uploads
Pillow>=10.0              # server-side upload validation (verify + MIME sniff)
